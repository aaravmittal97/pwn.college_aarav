challangs 7 touching files
```
we can creat a file by the command `touch`,so in the challenge we have to create two files `/temp/pwn` and `/tmp/college` and run `/challenge/run` to get the  flag.
```
## solutions:

we will first direct to the `/temp` directory through `cd` command then to create two files we will use `touch` command then run the command `/challenge/run` through this we will get our flag.

### commands run:

hacker@commands~touching-files:/$ cd /tmp
hacker@commands~touching-files:/tmp$ touch pwn
hacker@commands~touching-files:/tmp$ touch college
hacker@commands~touching-files:/tmp$ /challenge/run
Success! Here is your flag:


## flag:
```
pwn.college{clEOCwcbHHGwX7FRDvFYcjYVqNx.QXwMDO0wyMzAzNzEzW}
```
