challenge 8 removing files
```
`rm` is the command for removing any file ,a `delete_me` file will be created in the home directory we have to delete it and run `/challenge/check` which will make sure that the file is deleted and give the flag.
```
## solution:
we will delete the `delete_me` file by the command `rm delete_me` and then run the command `/challenge/check` which will check  if the file is deleted or not and will give the flag.

### commands run:

hacker@commands~removing-files:~$ rm delete_me
hacker@commands~removing-files:~$ /challenge/check
Excellent removal. Here is your reward:

## flag:
```
pwn.college{YCZGZT93RZiDaaU4Yy0jda2lRcH.QX2kDM1wyMzAzNzEzW}
```
