# challenge 10 hidden files
```
`ls` command doesn't list all the files by default  Linux has a convention where files that start with a `.` don't show up by default in ls and in a few other contexts. To view them with ls, we need to invoke `ls` with the `-a` flag, as so:
```
## solution:

we will first open the `/` directory by `cd` command then by using `ls -a` command we will see all the files in that directory,there we will find 
`.flag-27961402014340` which conatins the flag, when we will `cat` it ,it will give us the flag.

## flag:
``` 
pwn.college{INJPcz6m7N8AC9GJG5KwHS-cwe8.QXwUDO0wyMzAzNzEzW}
```
