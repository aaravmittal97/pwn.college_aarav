challenge 3 more catting practice
in the challenge we are given an absolute path which cannot be called using cd therefore we have to cat it as we cannot change the directories as well.

## soluition:
we will simply cat the file given to us that is `(/usr/share/dbus-1/flag)` as it is the absolute path and we will get the flag

### commands run:

hacker@commands~more-catting-practice:~$ cat /usr/share/dbus-1/flag

## flag:
```
pwn.college{gpUD0nwtwNSnjxrJUxGZNML7GL3.QXwITO0wyMzAzNzEzW}
```
