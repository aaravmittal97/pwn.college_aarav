challenge 1 cat:not the pet,but the command!
```
in the `cat` basically read the files, the flaf is copied to the flag file in the home directory,read it with cat
```

## solution:
here we just have to invoke the command `cat ./flag` by this well get our flag.

### commands run:

hacker@commands~cat-not-the-pet-but-the-command:~$ cat ./flag

## flag:
```
pwn.college{oPAaOl4U_gWHSmcua0oPts4rQDD.QXxcTN0wyMzAzNzEzW}
```
