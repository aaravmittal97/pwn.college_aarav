# challenge 8 reading files 
```
in the challenge we have to read `/challenge/read_me into the PWN environment variable,The /challenge/read_me will keep changing, so you'll need to read it right into the PWN variable with one command!
```
## solution:

we will invoke the command `read PWN < /challenge/read_me` and we will get the flag

### commands run:

hacker@variables~reading-files:~$ read PWN < /challenge/read_me
You've set the PWN variable properly! As promised, here is the flag:

## flag:
```
pwn.college{ETLoNs1XrrURpzySkbH5rM1NjX8.QXwIDO0wyMzAzNzEzW}
```
