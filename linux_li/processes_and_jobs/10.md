
# Challenge process exit codes

In this challenge, you must retrieve the exit code returned by `/challenge/get-code` and then run `/challenge/submit-code` with that error code as an argument. 

## Solution:

when we will run `/challenge/get-code` we will get a exiting code which we will access by `echo $?` which we will use as an argument for `/challenge/submit-code` and will get the flag
### Commands Run:

```sh
hacker@processes~process-exit-codes:~$ /challenge/get-code
Exiting with an error code!
hacker@processes~process-exit-codes:~$ echo $?
54
hacker@processes~process-exit-codes:~$ /challenge/submit-code 54
CORRECT! Here is your fla
```

## Flag: 

```
pwn.college{4lcYwwnD53vGUEh6C4CN1b3SYyp.QX5YDO1wyMzAzNzEzW}
```



