# challenge 2 matching with ?
```
Starting from your home directory, change your directory to /challenge, but use the ? character instead of c and l in the argument to cd! Once you're there, run /challenge/run for the flag
```
## solution:
we will firstly change the directory to `/challenge` by globbing and using `?` in place  of `a and l` and then run the command `/challenge/run` which will give the flag

### commands run:
```
hacker@globbing~matching-with-:~$ cd /?ha??enge
hacker@globbing~matching-with-:/challenge$ /challenge/run
You ran me with the working directory of /challenge! Here is your flag:
```
## flag:
```
pwn.college{c6W_2yLCWtE_UR3NVE59LFZOeF2.QXyIDO0wyMzAzNzEzW}
```

