challenge 9 home sweet home
`~`  is the home directory ,in the challenge we will have to specify a file in which our flag will be copied in the file it must a argument not more than 3 characters and it should be inside the home directory and must be an absolute path through this will get the flag ,`~`  is the home directory 

## solution:
while following the absolute path we will invoke the command `/challenge/run` and specify the argument `~/a` in which our flag will be copied

### commands run:

hacker@paths~home-sweet-home:~$ /challenge/run ~/h
Writing the file to /home/hacker/h!
... and reading it back to you:

## flag:
```
pwn.college{sQow5YTUo5VI-W8h8s3cH9KIFSW.QXzMDO0wyMzAzNzEzW}
```
