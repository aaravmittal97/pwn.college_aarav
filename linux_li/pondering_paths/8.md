challenge 8 implicit relative path
in the challenge the `cwd` is `/challenge`,`.` means the current directory so in this challenge for the flag  we have to we will have to find a relative path to invoke the `run` command.

## solution:
firstly we will execute the `/challenge` directory,now our cwd will be `/challenge` by following the relative path by running `./run` command we will get the flag

### commands run:

hacker@paths~implicit-relative-path:~$ cd /challenge
hacker@paths~implicit-relative-path:/challenge$ ./run
Correct!!!
./run is a relative path, invoked from the right directory!

## flag:
```
pwn.college{U3WBp23Dnzir5cUPosm2iy_5v5g.QXxUTN0wyMzAzNzEzW}
```
