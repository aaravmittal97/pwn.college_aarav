challenge 6 implicit relative paths,from/
in the challenge `cwd` means the current working directory for which we will have relative, here we have to fllow thw relative path while the `cwd` is `/` and get the flag.

### solution: 
the `cwd` is `/` so the relative path will be `challenge/run`,for this we will firstly navigate to `/` directory and execute the command `challenge/run` which will give us the flag.

### commands run:

hacker@paths~implicit-relative-paths-from-:~$ cd /
hacker@paths~implicit-relative-paths-from-:/$ challenge/run
Correct!!!
challenge/run is a relative path, invoked from the right directory!

## flag:
```
pwn.college{cupVsKfanMWs-CD4mQsEZCKQo6h.QX5QTN0wyMzAzNzEzW}
```
