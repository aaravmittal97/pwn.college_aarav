challenge 7 explicit relative paths,from/

in the challenge your `cwd` will be `/` and `challenge` should be called with a relative path that explicitly starts with a `.`

## solution:
`.` this refers to the same directory so we will invoke `./challenge` command to get the flag

### commands run:

hacker@paths~explicit-relative-paths-from-:~$ cd /
hacker@paths~explicit-relative-paths-from-:/$ ./challenge/run
Correct!!!
./challenge/run is a relative path, invoked from the right directory!

## flag:
```
pwn.college{QixyquuZYWEblt9scXLSsakHIUz.QXwUTN0wyMzAzNzEzW}
```


