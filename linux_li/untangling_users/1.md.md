
# Challenge becoming root with su

this challenge_does_ have a `root` password. That password is `hack-the-planet`, and you must provide it to `su` to become `root`! Go do that, and read the flag!

## Solution:

we will run the command `su` and provide it with the password `hack-the-planet` which give us access to the root and then we will `cat` the flag

### Commands Run:

```sh
hacker@users~becoming-root-with-su:~$ su
Password: 
root@users~becoming-root-with-su:/home/hacker# cat /flag

```

## Flag: 

```
pwn.college{UZxaMURI8BpojTUp2hskFhslUrR.QX1UDN1wyMzAzNzEzW}
```



